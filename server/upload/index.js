const uploadRoute = require('./upload.route');
const uploadController = require('./upload.controller');
const uploadService = require('./upload.service');

module.exports = { uploadRoute, uploadController, uploadService };